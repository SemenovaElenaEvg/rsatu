"use strict";

function pacientAuth() {
    var
        login = "",
        password = "";


    login = prompt("Введите логин");
    password = prompt("Введите пароль");

    // метод запроса к БД
}

function doctorAuth() {
        var
        login = "",
        password = "";

    login = prompt("Введите логин");
    password = prompt("Введите пароль");

    // метод запроса к БД
}